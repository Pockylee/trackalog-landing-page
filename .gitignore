# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
.pnpm-store

# next.js
.next
out
# production
build

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# testing
coverage

# Optional: If you use VS Code, you might want to ignore these
.vscode

# Optional: If you are using a package manager's lock file. (only use the one you're using)
yarn.lock
package-lock.json
pnpm-lock.yaml

# Optional: If using docker compose
docker-compose.override.yml

# Optional: If you use visual studio
*.suo
*.pdb
*.user
*.userosscache
*.sln

# Optional: if using typescript or other transpilers
*.tsbuildinfo
